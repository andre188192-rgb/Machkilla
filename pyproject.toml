[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "machkilla"
version = "0.1.0"
description = "Core engine for Machkilla"
requires-python = ">=3.10"
